apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: update-manifests-helm-octopub-test
  annotations:
    argo.octopus.com/project: "update-manifests-helm"
    argo.octopus.com/environment: "test"
spec:
  project: default
  source:
    repoURL: https://github.com/eddymoulton/octopus-argo-cd-samples
    targetRevision: HEAD
    path: manifests/update-manifests-helm-octopub/test/
  destination:
    server: https://kubernetes.default.svc
    namespace: update-manifests-helm-octopub-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
