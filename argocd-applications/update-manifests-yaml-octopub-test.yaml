apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: update-manifests-yaml-octopub-test
  annotations:
    argo.octopus.com/project: "update-manifests-yaml"
    argo.octopus.com/environment: "test"
spec:
  project: default
  source:
    repoURL: https://github.com/sathvikkumar-octo/octopus-argo-cd-samples
    targetRevision: HEAD
    path: manifests/update-manifests-yaml-octopub/test/
  destination:
    server: https://kubernetes.default.svc
    namespace: update-manifests-yaml-octopub-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
